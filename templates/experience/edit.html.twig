{% extends 'layoutAdmin.html.twig' %}

{% block title %}Edit Experience{% endblock %}

{% block body %}
<main class="container mt-4">
    <h1>Edit Experience</h1>

    {{ include('components/_form.html.twig', {'button_label': 'Update'}) }}

     {{ include('experience/_delete_form.html.twig') }}
        <a href="{{ path('experience_index') }}" class="btn btn-dark mt-5">Back to the list</a>
    </main>
{% endblock %}
