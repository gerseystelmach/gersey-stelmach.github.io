{% extends 'layoutAdmin.html.twig' %}

{% block title %}Experience
{% endblock %}

{% block body %}

    <main class="container mt-5">

        <div class="text-center">
            <h1 class="my-5"><span class="text-secondary">Gersey Stelmach</span> <span class="text-primary">Admin Panel</span></h1>
            <div>
                <a href="{{ path('skill_index') }} " class="btn btn-secondary fw-bold my-2 w-25">Skills</a>
            </div>
            <div>
                <a href="{{ path('experience_index') }}" class="btn btn-secondary fw-bold my-2 w-25">Experiences</a>
            </div>

            <div>
                <a href="{{ path('portfolio_index') }}" class="btn btn-secondary fw-bold my-2 w-25">Portfolio</a>
            </div>
            <div>
                <a href="{{ path('category_index') }}" class="btn btn-secondary fw-bold my-2 w-25">Category</a>
            </div>
            <div>
                <a href="{{ path('article_index') }}" class="btn btn-secondary fw-bold my-2 w-25">Blog</a>
            </div>
            <div class="my-5">
                <a href="{{ path('home') }}" class="btn btn-primary w-25 fw-bold">My website</a>
            </div>
        </div>

    </main>
{% endblock %}
