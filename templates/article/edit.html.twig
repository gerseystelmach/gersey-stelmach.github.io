{% extends 'base.html.twig' %}

{% block title %}Edit Article{% endblock %}

{% block body %}
<main class="container mt-5">
    <h1>Edit Article</h1>

    {{ include('components/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('article_index') }}" class="btn btn-dark">back to list</a>

    {{ include('article/_delete_form.html.twig') }}
    </main>
{% endblock %}
