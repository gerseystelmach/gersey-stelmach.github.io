{% extends 'layoutAdmin.html.twig' %}

{% block title %}Edit Category
{% endblock %}

{% block body %}
    <main class="container mt-5">
        <h1>Edit Category</h1>

        {{ include('components/_form.html.twig', {'button_label': 'Update'}) }}

        {{ include('category/_delete_form.html.twig') }}

        <a href="{{ path('category_index') }}" class="btn btn-dark mt-5">Back to the list</a>
    </main>
{% endblock %}
